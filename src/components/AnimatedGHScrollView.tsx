import Animated from 'react-native-reanimated';
import { ScrollView as GHScrollView } from 'react-native-gesture-handler';

export const AnimatedGHScrollView = Animated.createAnimatedComponent(GHScrollView);